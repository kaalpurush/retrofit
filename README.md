# retrofit-kotlin
Retrofit HTTP API Example Project with Kotlin.

1. Uses android view model with live data.
2. Example of shared and non-shared data repository between activities.
3. Example of Navigation Graph with proper fragment handling (toolbar title changes with fragment etc.) and deep link support.
4. Example of offline api sync through WorkManager
5. Sqlite fallback for api data while device is offline
